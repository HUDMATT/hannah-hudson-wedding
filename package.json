{
  "name": "hannah-hudson-wedding",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-pages.js",
    "dev": "npm run build && wrangler pages dev dist",
    "d1:migrate:local": "wrangler d1 migrations apply hannah-hudson-wedding-db --local",
    "d1:migrate:remote": "wrangler d1 migrations apply hannah-hudson-wedding-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
